package ron;

public class ModelParameters {
  float alpha = 50f / 100; // usual value is 50 / K
  float beta = 0.1f;// usual value is 0.1
  int topicNum = 100;
  int iteration = 2;
  int saveStep = 1;
  int beginSaveIters = 1000;
}